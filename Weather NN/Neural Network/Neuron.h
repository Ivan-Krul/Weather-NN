#pragma once
#include "PointMassiveLib.h"
#include "../Define.h"

template<typename _var> class Neuron {
	_var NeuronValue;
	size_t WeigthSize;
	_var* Weigth;

	public:
	Neuron(size_t count_weigth,bool need_rand) {
		if (need_rand) {
			
		}
		else {

		}
	}
};


